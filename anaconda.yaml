anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: arminbiere/cadical
      use_max_tag: true
      include_regex: ^rel-\d+\.\d+\.\d+$
      from_pattern: ^rel-(.+)$
      to_pattern: \1
      exclude_regex: (?i)^(?:sc\d+|sr\d+)$
